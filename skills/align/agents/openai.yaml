interface:
  display_name: "Align"
  short_description: "Settle every material decision before acting"
  default_prompt: "Use $align to build on our prior context and settle every remaining material decision with me before acting on this request."
policy:
  allow_implicit_invocation: false
