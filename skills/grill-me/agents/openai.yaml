interface:
  display_name: "Grill Me"
  short_description: "Settle every material decision before acting"
  default_prompt: "Use $grill-me to settle every material decision with me before acting on this request."
policy:
  allow_implicit_invocation: false
