interface:
  display_name: "Context Handoff"
  short_description: "Create tight prompts for fresh agent handoffs"
  default_prompt: "Use $context-handoff to prepare a fresh agent to review the current diff."

policy:
  allow_implicit_invocation: false
