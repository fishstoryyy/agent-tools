interface:
  display_name: "Grill Me Companion"
  short_description: "Coach a separate live Claude Code session"
  default_prompt: "Use $grill-me-companion with this Claude Code session JSONL path, follow only its active branch, and help me understand and steer the conversation."

policy:
  allow_implicit_invocation: false
