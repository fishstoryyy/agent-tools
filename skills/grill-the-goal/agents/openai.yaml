interface:
  display_name: "Grill the Goal"
  short_description: "Turn fuzzy goals into decision-ready briefs"
  default_prompt: "Use $grill-the-goal to turn my vague goal into a decision-ready brief before planning implementation."

policy:
  allow_implicit_invocation: true
