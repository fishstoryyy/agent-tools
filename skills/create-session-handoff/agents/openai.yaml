interface:
  display_name: "Create Session Handoff"
  short_description: "Create durable session continuation handoffs"
  default_prompt: "Use $create-session-handoff to create a durable handoff for continuing this work in a fresh session."

policy:
  allow_implicit_invocation: false
