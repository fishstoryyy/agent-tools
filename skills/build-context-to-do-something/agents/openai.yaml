interface:
  display_name: "Build Context to Do Something"
  short_description: "Build concise prompts for context-free agents"
  default_prompt: "Use $build-context-to-do-something to prepare a fresh agent to review the current diff."

policy:
  allow_implicit_invocation: false
